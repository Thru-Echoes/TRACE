# Round 2 — Independent Correctness Verification of the Round-1 Synthesis + Plan

**Date:** 2026-05-18
**Role:** Independent adversarial Round-2 correctness verifier. No prior context trusted; every finding and every proposed fix re-derived from code + read-only in-process probes.
**Method:** Source read of `decision_tools.py` / `session_tools.py` / `query_tools.py` / `prov_jsonld.py` / `store.py` / `embeddings.py` / `server.py`; read-only `git log -S` / `git diff`; in-process probes via the project `.venv` (`PYTHONPATH=src /Users/.../.venv/bin/python -c`, in-memory storage shim, no disk except `/tmp` scratch, no file in repo modified except this one); targeted `uv run pytest <nodeid>`; `ruff`; `pyright`. No `trace_*`/MCP calls. No processes signalled. No directory moved/renamed/deleted.

---

## 1. Verdict

**The synthesis is substantially correct. Confidence: ~92%.**

All six convergent CRITICAL/HIGH findings (G1–G6) are **independently CONFIRMED** by code reading + probes — including the two I was able to reproduce dynamically (G1 false-positive, G2 core-tool break). The three partials (A8/A4/A9) are confirmed PARTIAL. The §0 "FM7-is-a-flake / not a code regression" conclusion is **CONFIRMED** (in-process `trace_end_session` on the exact FM7 input = 0.000215s, correct). P9a (eager model2vec/embedding load at server startup) and P9b (no cross-process lock on the shared knowledge store; lost-update risk) are both **CONFIRMED**.

The proposed plan P1–P9 is mostly correct, but **P1 has a real, un-flagged regression risk** (its wording would suppress the §3-verified-solid ai→ai backward-compat warning on single-actor sessions unless the multi-actor guard is scoped to the non-ai branch only). **P2 is correct but under-specifies** the second coupling site (`query_tools.py:340` is already guarded; the real residual is that *both* should ideally route through `extension_hooks.py`, not just be try/excepted). Minor synthesis wording overreaches are corrected in §4 (notably: spec §3.6 is **correct** and the code *contradicts* it — the synthesis G5 lumps spec §3.6 in with the false claims, which is imprecise; and A8 is **further along** than the synthesis prose implies — a dedicated non-warning hint tier *was* added, the defect is a *duplicate* ⚠️ entry).

None of these corrections change the bottom line: **v0.4.1 is NOT release-ready (~75–80%); G1–G4 are real release blockers; do NOT "fix" FM7 as a code regression; do NOT bump the schema/protocol version.**

---

## 2. Finding-by-finding verdict table

| ID | Verdict | Evidence | file:line |
|----|---------|----------|-----------|
| **G1** | **CONFIRMED** | VERIFIED (ran in-process probe): single-actor `system→system`, `participants=[]` → FM1 fires `"Same actor instance proposed and resolved this decision"` + FM25 fires `"self-resolved in 0.0s"` + session-end `attribution_warning_count == 1`. Solo-human `human→human` same id (FM25 isolated out) → FM1 fires + `attribution_warning_count == 1`. A1 requires **0** for both. Multi-actor guard absent: `git log -S participants -- decision_tools.py session_tools.py` shows only `50051ec`(v0.3)/`4c9abd5`; grep shows `participants` appears **nowhere** in `decision_tools.py` and only in `start_session`/`create_session` parsing in `session_tools.py` (`:462,471,480,497,507`), never in the detector logic. `multi.actor` appears only as warning text (`decision_tools.py:97`, `session_tools.py:371`). | `decision_tools.py:80-83` (FM1 `is_self_resolution`), `:104` (FM25), `session_tools.py:330-334` (L5.4 structural) |
| **G2** | **CONFIRMED** | VERIFIED (ran read-only meta-path probe blocking only `trace_mcp.extensions.learn.store`, no dir moved): `query_tools` imports fine (function-local), but `project_summary(...)` → **`ModuleNotFoundError: No module named 'trace_mcp.extensions.learn.store'`**. `health_check(...)` returns normally (its `try/except` at `:339-344` catches it). Asymmetry exactly as synthesis states. `:158` import is unguarded; `:320` calls it unconditionally. | `query_tools.py:158` (unguarded `from trace_mcp.extensions.learn.store import load_store`), `:320` (`_compute_knowledge_metrics(project)` unconditional), `:339-344` (health_check guarded — contrast) |
| **G3** | **CONFIRMED** | VERIFIED (ran `grep -rn "trace_20260513_446733\|446733\|waggle" tests/`): zero matches for the session id / `446733`. Only "waggle" hits are prose in docstrings (`test_v041_attribution_audit.py:9`, `test_decision_guards.py:91`). Session JSON exists at `audit_2026-05-13_waggle_session/trace_session_trace_20260513_446733.json` but no test loads it. FINAL plan L9.1 (line 145) declared this gate mandatory ("invalid by construction" without it). | `audit_2026-05-13_waggle_session/trace_session_trace_20260513_446733.json` exists; `tests/` has zero references |
| **G4** | **CONFIRMED** | VERIFIED (ran `git diff 50051ec d20be80 -- tests/test_decision_guards.py`): `test_human_self_resolves_clean` (docstring *"Human proposes + human resolves -> no warning (humans can change their mind)"*) was **renamed to** `test_human_self_resolves_warns` with assertion inverted to `assert "Same actor instance proposed and resolved this decision" in result`. `_make_session` (`:28-38`) passes **no `participants`** → single-actor. So the test asserts the warning *fires* on a single-actor session = the A1-rejected behavior. Ran `uv run pytest ::test_human_self_resolves_warns ::test_human_different_instance_self_resolves_clean` → **2 passed** (green suite encodes the bug, masking G1). `test_human_different_instance_self_resolves_clean` passes only via id-inequality, not via A1's guard. | `tests/test_decision_guards.py:84-114` (`test_human_self_resolves_warns`), `:28-38` (`_make_session`, no participants), `:116-138` (different-instance, id-inequality only) |
| **G5** | **CONFIRMED (with a correction — see §4)** | VERIFIED (read): CHANGELOG `:24` *"counts decisions where `proposed_by == resolved_by` (same Actor instance) **in multi-actor sessions**"* and `:45` *"warns when `proposed_by == resolved_by` for any same-instance pair **in a multi-actor session**"* — both falsely claim multi-actor scoping the code lacks (refuted by G1 probe). Code docstrings `decision_tools.py:97`, `session_tools.py:166,371` say "in multi-actor workflows". **Correction:** spec §3.6 (`docs/specification.md:233`) is itself **correct** — it states the rule "...when the workflow involves multiple actors. In single-actor workflows... the human typically resolves decisions proposed by the AI." So the code *contradicts/over-enforces* §3.6; §3.6 does not "falsely claim". Synthesis G5's inclusion of "spec §3.6" in the falsely-claims list is imprecise (Reviewer A's F3 framing — "implementation is stricter than the spec it cites" — is the accurate one). | `CHANGELOG.md:24,45`; `decision_tools.py:97`; `session_tools.py:166,371`; `docs/specification.md:233` (spec is CORRECT) |
| **G6** | **CONFIRMED** | VERIFIED (ran `git tag -l \| wc -l` → `0`; `git for-each-ref refs/tags \| wc -l` → `0`). CHANGELOG header `:10` = `## [0.4.1] — In progress`. Compare links `:172-176` reference `v0.4.0`/`v0.3.0`/`v0.2.0`/`v0.1.0`/`releases/tag/v0.1.0` — none exist; all 404. | `CHANGELOG.md:10,172-176`; zero git tags |
| **A8** | **CONFIRMED PARTIAL (synthesis understates progress — see §4)** | VERIFIED (ran probe: 3 innocuous descriptions "turned out cleaner"/"Discovered files reorganized"/"CI turned out green" → `orphan_discovery_hint_count: 3`, all false positives). Field rename `_warning_count→_hint_count` **done** (`session_tools.py:104`). **Render produces BOTH**: a non-⚠️ structured "Orphan-discovery hints (v0.4.1): ..." line (`:175-182` — the A8-intended lower-severity tier, *which was added*) AND a duplicate ⚠️-prefixed entry via `audit_warnings.append` (`:378-383`) rendered with ⚠️ at `:205-207`. So A8 is *partially honored* (a separate hint tier exists); the residual defect is the **duplicate ⚠️ warning**, not total absence of de-emphasis. | `session_tools.py:104` (rename), `:72-77` (`_DISCOVERY_PHRASES`), `:175-182` (non-⚠️ hint line — A8-intended), `:293-300` + `:378-383` (duplicate ⚠️ push), `:205-207` (⚠️ render) |
| **A4** | **CONFIRMED PARTIAL** | VERIFIED (read): exporter architecture genuinely correct — event-ID correction → `bundle["wasInvalidatedBy"]` with explicit "REPUDIATORY semantics, distinct from `wasRevisionOf`"; URI correction → `prov:Influence` blank node `qi_id = f"_:infl_{evt.id}_{idx}"` (**deterministic**, not Python `hash()`) + `prov:atLocation` + `prov:qualifiedInfluence`; `parent_event_id` → `prov:wasInformedBy`. NOT a `wasRevisionOf` shortcut. **But** `TestRoundTrip` (`tests/test_v041_prov_ld_split.py:437-459`) does only `json.loads` + `assert "@context"/"bundle" in parsed` — no rdflib/pyld/PROV-O/SHACL/JSON-LD-expansion. A4-mandated parser round-trip test absent. | `prov_jsonld.py:51-54,108-113,172-201`; `tests/test_v041_prov_ld_split.py:437-459` |
| **A9** | **CONFIRMED PARTIAL** | VERIFIED (ran probe: `revises_event_id="evt_999"` → `ValueError: Invalid event references... Dangling reference`, hard-reject at `append_event:662-665`). Spec §4.4 `:381` says only "SHOULD reference valid event IDs within the same session"; `:383` says "Consumers MUST tolerate dangling references (... may exist in a different session)". Grep for `hard.reject\|tighten\|deferred to.*v1\|future version` in spec → **zero**. A9-required note about the producer-side hard-reject tightening + v1.1 relaxation deferral is absent. Mild internal inconsistency: §4.4:383 tells consumers to tolerate cross-session dangling refs while the producer makes them unrepresentable. | `session_tools.py:662-665` (hard-reject); `docs/specification.md:381-383` (no tightening/v1.1 note) |
| **§0 FM7-is-a-flake** | **CONFIRMED (not a code regression)** | VERIFIED (ran in-process probe: `start_session` + 8 `category="learning"` annotations + `end_session`, FM7's exact input). Result: `trace_end_session` **alone = 0.000215s**, exception-free, correct output `"8 events: 8 annotations\n--- Attribution Audit ---\nNo contributions, decisions, or corrections to review."`. Independently corroborates Reviewer A's 0.001s. `_send_and_receive` default timeout = **15.0s** (`test_e2e_server.py:62,84`) on `proc.stdout.readline()` — a subprocess-read timeout, not in-process logic. No v0.4.1 code defect in the FM7 path. | `tests/test_e2e_server.py:62,84`; `tests/test_failure_modes_e2e.py:285-307`; in-process probe |
| **P9a** (eager embedding/model2vec import at startup) | **CONFIRMED** | VERIFIED (read import chain + ran timing probe). Chain: `server.py:758 _load_extensions()` → `:727 importlib.import_module` → `:729 mod.register(...)`; learn `__init__.py:20` does `from ...embeddings import get_embedding_provider` at module import; `register()` (`__init__.py:38`) calls `get_embedding_provider(_config)` **at startup**. `embeddings.py:40-45`: `from model2vec import StaticModel` at **module top-level** (real eager import despite `# noqa: F401`). `get_embedding_provider` `:158-162`: in `auto` mode w/ model2vec available & no OpenAI key → returns `Model2VecEmbeddingProvider`, whose `__init__` (`:97-104`) calls `StaticModel.from_pretrained(...)` (loads/downloads `potion-base-8M`) — eagerly at register(). Measured: `import model2vec` = 0.075s; `StaticModel.from_pretrained` warm-cache = **0.497s**. Cold HF cache adds a network download ("Fetching N files"). Each concurrent server subprocess independently does this (N×RAM + N× cold-load). Plausible >15s cold-start contributor under load. | `server.py:727-729,758`; `extensions/learn/__init__.py:20,38`; `embeddings.py:40-45,97-104,158-162` |
| **P9b** (shared knowledge store cross-process locking) | **CONFIRMED — no cross-process lock; lost-update risk real** | VERIFIED (read `store.py`). `save_store` (`:75-106`) uses atomic write (`tempfile.mkstemp` + `os.replace`) — prevents *torn* files only. **No `fcntl`/`flock`/`FileLock`/lock-file anywhere** in `store.py` (grep: zero). Pattern is read-modify-write: `load_store()` reads whole store → callers `add_learning`/`remove_learning` mutate in memory → `save_store()` atomically replaces the **entire file**. Two concurrent processes ⇒ classic lost-update (second `os.replace` clobbers the first's appended learnings — last-writer-wins on the whole file, not per-record merge). Store path `~/.trace/knowledge/` is shared across ALL sessions/projects. Per-session `~/.trace/sessions/*.json` are distinct files (synthesis correctly excludes them). | `extensions/learn/store.py:75-106` (atomic-but-unlocked), `:109-138` (in-memory mutate), `:30-43` (shared dir) |

**Static analysis re-verified:** `uv run ruff check src/` → "All checks passed!" (exit 0). `uv run pyright src/` → **0 errors, 3 warnings, 0 informations** (the 3 are the `# noqa` feature-detection probe imports in `embeddings.py:27,34,41`). `schemas/` contains **only** `trace-v0.4.json`. L1.3 single-source `trace_version` re-verified by probe: v0.3 blob loads with `Session.trace_version` preserved as `"0.3.0"` (NOT force-bumped), `Environment` has no `trace_version` attr, `context` stays `https://trace-protocol.org/v0.3`, unknown field dropped, round-trip preserves `0.3.0`. A6 `_is_explicit_absence` = `s.strip() in _EXPLICIT_ABSENCE_MARKERS` frozenset (`session_tools.py:64`). **§3 verified-solid items spot-checked TRUE.**

---

## 3. Fix-by-fix table (P1–P9)

| Fix | Verdict | Why |
|-----|---------|-----|
| **P1** (implement A1 multi-actor guard + restore `_clean` test + correct docs) | **CORRECT but INCOMPLETE / RISKY** | Direction is right (gate FM1/FM25/L5.4 on `proposed_by==resolved_by` **AND** ≥2 distinct actors w/ A7 empty-participants fallback) and would resolve G1+G4+G5. **Risk the synthesis does not flag:** in `decision_tools.py:85-99` the **ai→ai** backward-compat warning (`"AI resolved its own proposal"`, `test_ai_self_resolves_gets_warning`) shares the *same* `if is_self_resolution and not suppress` gate as the general branch. I verified by probe that ai→ai single-actor currently fires this warning — and §3 lists this v0.3 behavior as verified-solid (must preserve); A1 line 47 explicitly says "Keep ai→ai branch as-is (backward compat)". The synthesis P1 text says "fire... only when ... ≥2 distinct actor instances" with **no ai→ai carve-out** — a naive implementation gating the whole `is_self_resolution` block would **regress the §3-verified-solid ai→ai single-actor warning**. P1 MUST scope the multi-actor guard to the **non-ai (general-case) branch only** (and the L5.4 `attribution_warning_ids` push), leaving the ai-specific branch and the existing `self_resolved_ids` ai-only count ungated. Also: P1 should keep the `if resolved_by is not None`/short-circuit and the `TRACE_SUPPRESS_*` env path intact (both currently correct). With the ai→ai carve-out made explicit, P1 is correct. |
| **P2** (restore core/extension boundary; guard `:158`, audit `:340`; add delete-the-extension test) | **CORRECT but INCOMPLETE** | try/except-ImportError → omit knowledge metrics at `query_tools.py:158` does make `project_summary` a pure core tool and satisfies `evt_002` — verified that the `:340` health_check pattern (`try/except Exception`) already works under the probe. **Two gaps:** (1) `query_tools.py:340` is **already guarded** (try/except Exception, fallback) — it is *not* a current break; the synthesis/P2 should state the real residual is architectural (a *named-extension import from core* even if guarded), best routed through `extension_hooks.py` per Reviewer C C1, not merely try/excepted. P2's "(and audit `:340`)" overstates `:340` as broken. (2) The proposed test must assert all 18 *core tools function* with the extension absent **without moving the directory in CI** (use the same meta-path/sys.modules block I used, or a subprocess with a trimmed path) — Reviewer C's "physically mv the dir" experiment is not a safe CI pattern. Fix is correct; spec it as hook-routing (preferred) or guarded-import, and use a non-destructive absence simulation. |
| **P3** (write the mandated L9.1 waggle gate) | **CORRECT** | Loading `trace_session_trace_20260513_446733.json` under v0.4.1 and asserting the documented audit counts is exactly the FINAL-plan L9.1 contract and resolves G3. Caveat (does not make it wrong): the *exact* asserted counts in FINAL L9.1 (`missing_snippet_contribution_count=15`, `=1` correction, `attribution_warning_count≥2`, `orphan_discovery≥1`) were written pre-A1; once P1's multi-actor guard lands, the waggle session's `attribution_warning_count` must be re-derived against the *guarded* detector (the waggle session is multi-actor so it should still be ≥2 for evt_001+evt_025 — but P3 must compute expected counts from the post-P1 code, not copy the pre-A1 numbers blindly). Sequence P3 *after* P1, or write it to assert on the actual post-fix audit. |
| **P4** (de-noise orphan-discovery: move hint out of `audit_warnings`, tighten phrases) | **CORRECT (re-scope the wording)** | Resolves A8. **Refinement:** the fix is **not** "move the hint out of `audit_warnings` into a lower-severity tier" as if no such tier exists — a non-⚠️ structured hint line *already exists* (`session_tools.py:175-182`). The actual minimal fix is to **stop *also* appending to `audit_warnings`** at `:378-383` (delete the duplicate ⚠️ push), leaving only the existing hint line. P4 as worded would work but mis-describes the current state; the correct, smaller change is "remove the duplicate". Phrase-tightening is optional/orthogonal. Low risk; does not touch §3. |
| **P5** (add PROV-O parser round-trip test) | **CORRECT** | Architecture already correct (verified); only the test depth is missing. Adding an rdflib/pyld expansion + PROV-O constraint check on the emitted JSON-LD resolves A4 part 2. Pure test addition, zero production risk, does not touch §3. Note: requires adding `rdflib`/`pyld` (or equivalent) to dev deps — flag as a new dev dependency, not a runtime one. |
| **P6** (doc truth-up: spec §4.4 hard-reject + v1.1 deferral; ADR 002 in index; widen CONTRIBUTING; new boundary ADR/spec §) | **CORRECT** | Pure documentation; resolves A9 + C6/C7/C8. Adding the §4.4 normative note ("implementations MAY hard-reject out-of-session refs; the reference server does; cross-session support deferred to v1.1") also resolves the §4.4:383-vs-producer internal inconsistency I found. No code, no risk. |
| **P7** (release process, LAST, after P1–P3 + full green suite; decide tag policy; flip header) | **CORRECT** | Resolves G6. Correctly gated behind P1–P3 + a real green `uv run pytest`. Correctly flags the tag-policy decision (create `v0.1.0..v0.4.1` vs commit-range compare-links) for the user. Correctly branch+PR (never direct to main). Sound sequencing. |
| **P8** (FM7 = E2E load-fragility, NOT a code fix; make `_send_and_receive` timeout configurable / serialize MCP-subprocess E2E) | **CORRECT** | Matches my §0 confirmation. The proposed harness hardening (parameterize the 15s timeout / mark MCP-subprocess E2E serial-or-skip-under-load / surface `proc.stderr` on timeout) is the right shape and explicitly does **not** modify `trace_end_session`. Low risk, optional/parallel. |
| **P9** (lazy-load embedding model; verify shared-knowledge-store concurrency) | **CORRECT — both sub-claims verified; (b) needs a concrete remedy** | (a) Lazy-loading the embedding provider on first `recall`/`extract` rather than at `register()` is technically correct and resolves the verified P9a eager-load (server cold-start + N×RAM). **One caution:** the eager `from model2vec import StaticModel` at `embeddings.py:41` (module top-level) is also a feature-detection probe setting `_HAS_MODEL2VEC`; making provider *instantiation* lazy is safe, but if the *module-level import* is also deferred, preserve the `_HAS_MODEL2VEC` capability flag semantics (don't break `get_embedding_provider`'s backend-selection). (b) is **confirmed real** (no cross-process lock; whole-file last-writer-wins lost-update on shared `~/.trace/knowledge/`). The fix must be concrete: add an OS advisory lock (`fcntl.flock` on POSIX / `msvcrt` on Windows — note the codebase deliberately avoids `fcntl` for the *session* path per CLAUDE.md, so a portable lock-file or `filelock` dep is the consistent choice) around the load-modify-save critical section in `store.py`, **or** explicitly document the last-writer-wins risk. P9b should not be closed by documentation alone if multiple concurrent sessions writing the same project's knowledge is a supported scenario (the MEMORY.md cross-project config implies it is). Sub-claims correct; (b) remedy must be specified, not just "verify". |

---

## 4. Corrections to the synthesis

1. **G5 over-broad — spec §3.6 is CORRECT, not a false claim.** The synthesis G5 row says *"CHANGELOG (24,45), `decision_tools.py:97`, `session_tools.py:166,371`, **spec §3.6** falsely claim the multi-actor scoping exists."* Verified: `docs/specification.md:233` states the rule **with** the multi-actor qualifier ("...when the workflow involves multiple actors. In single-actor workflows... the human typically resolves decisions proposed by the AI"). The **spec is the correct artifact**; the **code contradicts/over-enforces it**. Reviewer A's F3 framing ("implementation is stricter than the spec it cites") is accurate; the synthesis G5 wording incorrectly implicates §3.6. Practical consequence: P1's "correct CHANGELOG/spec §3.6/docstrings to match real behavior" should be "correct CHANGELOG + docstrings to match the **fixed** code; **do not weaken spec §3.6** — the code must rise to meet it." Weakening §3.6 to admit the unconditional behavior would be the wrong direction (it would also discard A1's production-data argument). The plan §6 already says "do not bump version"; this clarifies the doc-fix direction.

2. **A8 understated — a low-severity hint tier WAS added; the defect is a DUPLICATE ⚠️ entry, not absence of de-emphasis.** The synthesis says "the hint is still pushed into `audit_warnings` at ⚠️ warning severity ... A8's intent (de-emphasize) unmet." Verified by probe: the render emits **both** a non-⚠️ "Orphan-discovery hints (v0.4.1): ..." structured line (`:175-182`, the A8-intended tier — *this part of A8 landed*) **and** a duplicate ⚠️ entry (`:378-383` → `:205-207`). A8 is *partially honored*, not "intent unmet". The correct minimal P4 fix is to **delete the duplicate `audit_warnings.append`**, not "move the hint out". This is a scoping correction, not a severity-of-finding change (A8 PARTIAL stands).

3. **P2's "(and audit `:340`)" overstates the second site.** `query_tools.py:340` (`health_check`) is **already** wrapped in `try/except Exception` with a working fallback (`:339-344`) — verified by probe that `health_check` survives the extension being absent. It is **not a current break**. The real residual at `:340` is architectural purity (a named-extension import from core, even though guarded), which Reviewer C correctly frames as "should also route through a hook". P2 should not imply `:340` is a functional defect on par with `:158`.

4. **P1 missing ai→ai backward-compat carve-out (the most consequential omission).** Detailed in §3/P1. The synthesis P1 text would, if implemented literally, suppress the §3-verified-solid v0.3 ai→ai single-actor "AI resolved its own proposal" warning. A1 line 47 explicitly preserves the ai→ai branch; the synthesis P1 wording drops this. This is the one place where following the plan as written could break a §3 verified-solid item — exactly the failure class the verifier mission targets. **Flag for the implementer: gate only the non-ai general-case branch + the L5.4 push; leave the ai-specific branch and ai-only `self_resolved_ids` count ungated.**

5. **P3 asserted counts are pre-A1 and may not survive P1 verbatim.** Minor: FINAL L9.1's literal expected counts predate the multi-actor guard. P3 must derive expected counts from the post-P1 code (the waggle session is multi-actor so the attribution warnings should still fire — but P3 should compute, not copy). Sequence P3 after P1. Does not change G3's CONFIRMED status.

6. **P9b cannot be closed by "verify" alone — a concrete locking decision is required.** The synthesis P9 says "confirm cross-process locking, or document the ... risk and add locking." Verified: there is **no** cross-process lock and the lost-update risk is real on a store that MEMORY.md confirms is shared across 15 projects' sessions. Given concurrent multi-session use is the documented norm, "document the risk" alone is insufficient; P9 should mandate the lock (portable lock-file / `filelock`, consistent with the codebase's deliberate no-`fcntl` stance for session files). This strengthens, not refutes, P9.

7. **Nit (not a correction, a confirmation of synthesis accuracy):** the synthesis §0 attributes the FM7 timeout partly to "8+ concurrent live Claude sessions". I did not and cannot verify host load as the trigger from inside the repo (out of scope, and forbidden to signal/inspect processes), but I **independently confirmed the load-bearing technical claim** that matters: the FM7 *code path* is fast/correct in-process (0.000215s), so FM7 is not a v0.4.1 logic regression regardless of what specifically slows the subprocess cold-start. The eager model2vec load (P9a, confirmed) is a verified, code-resident plausible contributor. The synthesis conclusion stands; only the "8+ sessions" specific is INFERRED, not VERIFIED, which the synthesis itself already hedges.

---

## 5. Bottom line

The synthesis correctly identifies the release blockers and correctly refutes the FM7-code-regression hypothesis. **G1, G2, G3, G4, G5, G6 all CONFIRMED** (G5 with the §3.6 wording correction); **A8/A4/A9 PARTIAL confirmed** (A8 further along than stated); **§0 CONFIRMED**; **P9a + P9b CONFIRMED**. The plan is sound except: **P1 must explicitly preserve the ai→ai single-actor backward-compat warning** (un-flagged §3-regression risk — the single most important correction), **P2 over-states `:340` as a break** and should mandate hook-routing + a non-destructive absence test, **P4** is a "remove the duplicate", not "add a tier", and **P9b** needs a mandated lock, not optional documentation. None of these change the verdict: **not release-ready (~75–80%); do not version-bump; do not touch `trace_end_session`; G1–G4 are blockers.**
