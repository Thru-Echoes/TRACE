# Round 3 — Independent Engineering-Reality Verification of the FINAL Remediation Plan

**Date:** 2026-05-18
**Role:** Independent Round-3 engineering-reality verifier (LAST verification before implementation). No prior context trusted; every P-item re-derived from the real current code via read-only probes.
**Target:** `review_2026-05-18_v041_status/round_FINAL_plan.md` (P1–P9), checked that all Round-2 corrections were correctly folded in and that each P-item is implementable EXACTLY as written.
**Method:** Full source read of `decision_tools.py`, `session_tools.py`, `query_tools.py`, `extensions/learn/__init__.py`, `extensions/learn/store.py`, `decision-audit.sh`, `server.py`; the 2 named test files; ADR 002; spec §4.4; CONTRIBUTING; `pyproject.toml`. Inline `uv run python -c` JSON re-derivation of the waggle counts + `evt_016` + waggle actor types. Targeted `uv run pytest` on 3 nodeids (3 passed). Read-only `git tag -l`, `grep`. No file written except this one. No process signalled. No `trace_*`/MCP calls. No directory moved.

---

## 1. Verdict

**GO-WITH-FIXES — confidence ~90%.**

The FINAL plan's central technical claims are **verified implementable against the real current code**, and the Round-2 corrections that change *outcomes* (P1 ai→ai carve-out, M3=actor-types, M2=exactly-2-tests, P2 `:340`-already-guarded, P4 delete-the-duplicate, P9b mandated) are **correctly folded in**. The P3 counts are **exactly correct** (independently re-derived from the JSON: 28 / 15 / 1 / 2). M3 / `evt_016` is **correctly resolved** to "≥2 actor TYPES" per A1's literal text, with the one divergent real case (multi-human same-self-resolve) properly surfaced as a USER open item rather than silently decided.

Three **NEEDS-CORRECTION** items remain, all of the same low-severity *doc/scope-precision* class the process has been converging on (none is a crash, none changes the GO direction, all are concrete unstated steps a literal implementer would miss):

1. **P1/P6 doc-truth-up omits `docs/adr/002-v041-protocol-additions.md:32` and `:100`.** ADR 002 D1:32 states the rule is "enforced at log time (FM1 generalized to **all same-instance pairs**) AND at session-end" and :100 says the hook generalizes "to **any same-instance pair**" — both become stale/over-broad after P1's multi-actor guard. The FINAL plan names only `CHANGELOG.md:24,45` + the two `.py` docstrings (P1) and ADR-index/CONTRIBUTING/spec-§4.4 (P6). The ADR is the durable "why" home; leaving D1:32 + :100 stale re-creates exactly the G5 doc/code-contradiction the plan claims P1 resolves. **Round-2 exhaustiveness flagged this verbatim (A-F3 dilution / M1 / §4 / table line "ADR 002 D1 line 32 reconcile … ❌ P1 says docstrings"); the FINAL plan did NOT fold it in.**
2. **P9b's `store.py:75-106` file:line pin is narrower than the actual lost-update window.** `:75-106` is `save_store` (the write half). The lost-update critical section is the whole *load_store → mutate → save_store* span, which lives in `extensions/learn/__init__.py`'s hooks/tools (`_recall_hook`/`_extract_hook`/`trace_learn_add`/`forget`), not solely in `store.py:75-106`. A lock placed only inside `save_store` serializes writes but does NOT prevent lost-update (process B's `load_store` can still read stale state before A's `save_store` lands). Direction correct, citation under-specifies the region. (Round-2 correctness §3/P9(b) + exhaustiveness M6 noted the broader scope.)
3. **P5 does not state the new dev-dependency.** `pyproject.toml:29-38` `dev` has no `rdflib`/`pyld`; a real PROV-O parser round-trip test requires adding one. Pure-test, zero runtime risk, but the bullet omits the concrete step (Round-2 correctness §3/P5 raised exactly this; still unstated in FINAL).

None of these change the bottom line. P1–P4, P7, P8, P9a are implementable **exactly as written**. The plan is safe to execute after adding the ADR-002-D1/:100 target to P1's (or P6's) doc list, widening P9b's locked-region description from "`store.py:75-106`" to "the load→mutate→save span", and noting P5's dev-dep.

---

## 2. Per-P-item implementability table

| P-item | Verdict | Evidence (file:line; VERIFIED ran / INFERRED read) |
|---|---|---|
| **P1** | **IMPLEMENTABLE** (with the ADR-002 doc target added — see §5.1) | **VERIFIED (read + ran).** `decision_tools.py:85-99` ALREADY has a clean `if resolved_by_type == "ai":` (`:86-91`, message "AI resolved its own proposal") / `else:` (`:92-99`, message "Same actor instance proposed and resolved this decision … spec §3.6"), both inside `if is_self_resolution and not suppress:` (`:85`). `is_self_resolution` (`:80-83`) is pure `(type,id)` equality — NO multi-actor guard (G1 confirmed). The guard attaches to the `else:` branch (`:92-99`) ONLY; the ai→ai branch (`:86-91`) is structurally separate and stays unconditional. FM25 (`:104`) is a sibling `if elapsed<5.0 and is_self_resolution and not suppress` — same guard, same scoping. `session_tools.py`: the ai-only `self_resolved_ids` count (`:320-323`, `proposed_by.type==resolved_by.type=="ai"`) is structurally separate from the L5.4 generalized detector (`:330-334`, full `(type,id)` equality); the guard goes on `:330-334` only, leaving the ai-only count ungated. **Ran:** `test_ai_self_resolves_gets_warning` PASSES on current code (hits `:86-91`, ungated → survives P1); the 2 bug-encoding tests (`test_human_self_resolves_warns`, hook `test_hook_handles_session_with_human_self_resolution`) PASS on current code (encode the rejected behavior). M2-surface = **exactly these 2** tests: `test_v041_attribution_audit.py::_make_session` (`:46-59`) has 2 participants (human + ai = 2 types) so `TestAttributionWarningDetector`/`TestRenderOrdering` survive P1 unchanged. **M1 hook expressibility VERIFIED:** `decision-audit.sh:26-89` embeds a Python block with the full `data` (incl. `metadata` + all `events`); the `:69` check `pb.type==rb.type and pb.id==rb.id` can be guarded by a session-level distinct-actor-TYPE set computed in the same block (`{a.type for participants} ∪ {e.actor.type for events}`) — fully expressible there. |
| **P2** | **IMPLEMENTABLE** | **VERIFIED (read + grep).** `query_tools.py:158` `from trace_mcp.extensions.learn.store import load_store` is unguarded, inside `_compute_knowledge_metrics` (`:152`), called UNCONDITIONALLY at `:320` `"knowledge": _compute_knowledge_metrics(project)` inside `project_summary`'s return dict. `:340` `from …store import _get_directory` is ALREADY inside `try/except Exception` (`:339-344`) — FINAL plan's "`:340` already guarded — no change" is correct. **Re-grep of `src/trace_mcp/` (excluding `extensions/`, `_load_extensions`, `iter_modules`, `extension_hooks`) returns ONLY `query_tools.py:158` + `:340`** — `:158` is genuinely the only unguarded core→ext import. Fail-open shape correct: `_compute_knowledge_metrics` returns a plain `dict`; `trace_project_summary` (`server.py:646-648`) does `json.dumps(summary)`. Wrapping `:158` in `try/except ImportError` returning a dict → `project_summary` renders cleanly with `"knowledge": {}`. (Server wrapper `:649` has an outer `except Exception` backstop; the targeted ImportError guard keeps `project_summary` itself a pure core tool.) |
| **P3** | **IMPLEMENTABLE** | **VERIFIED (ran JSON re-derivation — see §4).** All 4 asserted counts EXACT: 28 events / 15 missing-snippet contributions / 1 missing-snippet correction / 2 same-instance self-resolutions (`evt_001`,`evt_025`). **P3↔P1 interaction SAFE:** waggle union actor TYPES = `{human, ai}` = 2 → P1's "≥2 types" guard satisfied → `attribution_warning_count` stays = 2 ≥ 2 post-P1. Actor-ID drift (participants `ai/claude` vs events `ai/ai-assistant`) is harmless under type-based counting (both type `ai`) — exactly the `evt_016`/A1 rationale. Plan correctly sequences P3 after P1. L9.7-partial / L9.0-status documentation sub-points are doc-only, low-effort. |
| **P4** | **IMPLEMENTABLE** | **VERIFIED (read).** Low-severity tier ALREADY exists: `session_tools.py:175-182` renders a NON-⚠️ `"Orphan-discovery hints (v0.4.1): …"` line. The duplicate ⚠️ is exactly `:378-383` (`if orphan_discovery_ids: audit_warnings.append(...)`), rendered with ⚠️ via `:205-207`. Deleting `:378-383` is the correct minimal fix: it touches NO count field (`orphan_discovery_hint_count`/`_event_ids` set independently at `:431-432`); **VERIFIED no test asserts the orphan-discovery `audit_warnings`/⚠️ text** (`TestOrphanDiscoveryHint` asserts only `.orphan_discovery_hint_count`/`.orphan_discovery_event_ids`; the `[hint]` test assertions are the *dispatch*-visibility hint, a different feature). Zero test-rework surface. "Delete the duplicate" is exactly right, NOT "add a tier". |
| **P5** | **IMPLEMENTABLE** (state the dev-dep — §5.3) | **VERIFIED (read).** Exporter architecture confirmed correct elsewhere (Round-1/2). `pyproject.toml:29-38` `dev` has no `rdflib`/`pyld`. Pure test addition, zero runtime risk; the plan bullet omits the concrete "add `rdflib`/`pyld` to dev deps" step. Minor. |
| **P6** | **IMPLEMENTABLE** (also covers the ADR-002-D1 fix if §5.1 routed here) | **VERIFIED (read).** `docs/adr/002-v041-protocol-additions.md` exists as a file but `docs/adr/README.md:10` index lists ONLY `001` → adding the 002 row is trivial. `CONTRIBUTING.md:75` core list = `server.py, schema/, storage/, tools/` (omits `exporters/`, `scratchpad.py`). `specification.md:379-382` §4.4 SHOULD-only, no producer hard-reject / v1.1 note. All three doc edits implementable. **Gap:** the ADR-002-D1:32 + :100 staleness (see §5.1) is NOT in P6's list either — must be added to P1 or P6. |
| **P7** | **IMPLEMENTABLE** | **VERIFIED (ran).** `git tag -l` → 0 tags. Tag-policy decision correctly flagged for USER. Correctly gated behind P1–P3 + full green suite (run without load contention) + branch/PR-never-direct-to-main. Sound. |
| **P8** | **IMPLEMENTABLE** | **VERIFIED (read).** `tests/test_e2e_server.py:62` `timeout: float = 15.0`; `:84` `asyncio.wait_for(proc.stdout.readline(), timeout=timeout)` — a subprocess-read timeout, NOT in-process logic. "Make configurable / mark serial-or-skip-under-load; do NOT modify `trace_end_session`" is the correct shape. Optional/parallel. |
| **P9** | **IMPLEMENTABLE** (widen P9b region wording — §5.2) | **VERIFIED (read).** **(a)** `extensions/learn/__init__.py:20` does `from …embeddings import get_embedding_provider` at module import; `register()` (`:34`) calls `get_embedding_provider(_config)` at `:39` — eager at server startup. `_embedding_provider` is consumed ONLY inside nested async tool/hook bodies (`:47-345`), never at import time → lazy-loading is safe (no import-time side effect relied on elsewhere). The `_HAS_MODEL2VEC` capability-flag caution if the module-level import is also deferred is a known impl detail, not a plan defect. **(b)** `store.py:75-106` `save_store` = atomic temp+`os.replace` (torn-write-safe) but ZERO cross-process lock (no `fcntl`/`flock`/`FileLock` in `store.py`). Lost-update is real on the read-modify-write span. Lockfile mandate correct; the `:75-106` pin is the write half only (see §5.2). `:104` calls non-atomic `save_embeddings_cache` (M5/M6) — not blocking but worth the same fix. |

---

## 3. M3 / `evt_016` independent re-derivation — VERDICT: CORRECT

**The FINAL plan's M3 resolution ("≥2 unique actor TYPES, not instances, across `metadata.participants` ∪ event actors, fallback to event actors when participants empty") is correctly folded in and faithful to A1's authoritative text.**

- A1 (`trace_v1_round3_amendments.md:39`) literal text: *"AND the session involves multiple distinct actors (**≥2 unique actor types** across `metadata.participants` and event actors)"*. The FINAL plan line 14 quotes this **exactly**. (Round-1 SYNTHESIS line 41 had said "≥2 distinct actor **instances**" — the looser/wrong wording; the FINAL plan **correctly switched to A1's "types"**, which is the Round-2-exhaustiveness-M3 fix.)
- TRACE decision `evt_016` (session `trace_20260518_5dd958`) — **VERIFIED `proposed_by=ai/claude`, `resolved_by=None`, `disposition=proposed`** (AI proposal left in `proposed`, flagged to USER). The FINAL plan correctly lists it as an Open Item for USER and does NOT self-resolve it — consistent with the attribution protocol and with M3 being "outcome-determining, confirm or override".
- **Independent re-derivation against A1's named cases (with the `is_self_resolution` type-AND-id precondition):**
  - Solo human `human→human`, single-actor → types `{human}` = 1 → NO warn. ✅ (A1 wants none.)
  - `system→system` single-actor → types `{system}` = 1 → NO warn. ✅
  - Multi-AI `(ai,claude)→(ai,gpt)` → `is_self_resolution`=False (ids differ) → never reaches warn; also types `{ai}` = 1 → double-safe NO warn. ✅
  - evt_025 same human, genuinely multi-actor session (human + ai present) → types `{human,ai}` = 2 → WARN. ✅ (A1 line 47 wants it.)
- **The one real case where type-based and instance-based DIVERGE:** a multi-human, no-AI session where `human/alice` self-resolves while `human/bob` also participated. Type-based: types `{human}` = 1 → NO warn (suppresses a warning that arguably should fire). Instance-based: instances `{alice,bob}` = 2 → WARN. **This divergence is real but is NOT a hidden bug** — `evt_016`'s description explicitly documents the type-vs-instance choice and its consequence, sources it to A1's literal text, and the FINAL plan surfaces M3 as a USER open item ("resolved to ≥2 actor TYPES per A1 — confirm or override (outcome-determining)"). A1's own false-positive list prioritizes not-firing on solo-human/multi-AI and does not enumerate the multi-human case; "≥2 types" is the faithful reading. **Verdict: the `evt_016` resolution does not create a wrong outcome the plan conceals; the residual ambiguity is correctly escalated, not silently decided.**

---

## 4. P3 count re-derivation — plan's numbers vs mine

Independently re-derived from `audit_2026-05-13_waggle_session/trace_session_trace_20260513_446733.json` by replicating `_build_attribution_audit`'s exact logic (`uv run python -c`, read-only):

| Count | Plan asserts | My re-derivation | Match |
|---|---|---|---|
| Total events | 28 | **28** (decision: 2, annotation: 9, contribution: 17) | ✅ EXACT |
| Missing-snippet contributions | 15 | **15** (17 contributions; 15 with `conversation_snippet is None`; 0 explicit-absence) | ✅ EXACT |
| Missing-snippet corrections | 1 | **1** (1 correction total; 1 missing snippet) | ✅ EXACT |
| Same-instance self-resolutions | 2 | **2** → `evt_001`, `evt_025` (both `human/human→human/human`; 0 ai-only) | ✅ EXACT |

Plus the **P3↔P1 safety check** (VERIFIED): waggle union actor TYPES = `{human, ai}` = 2 ≥ 2 → P1's guard is satisfied for this session → `attribution_warning_count` remains 2 post-P1 → the L9.1 gate's `attribution_warning_count≥2` assertion **survives the P1 guard**. The plan's P3 numbers are writable exactly as stated; sequence P3 after P1 (the plan does so).

All four FINAL-plan P3 counts are **exactly correct**. No correction needed to P3's asserted numbers.

---

## 5. Remaining plan bugs (NEEDS-CORRECTION)

### 5.1 P1/P6 doc-truth-up omits ADR 002 D1:32 + :100 (HIGH-confidence, low-severity)
`docs/adr/002-v041-protocol-additions.md:32`: *"The rule is enforced at log time (FM1 generalized to **all same-instance pairs**) AND at session-end…"* and `:100`: hook *"generalizes from `ai`-only … to **any same-instance pair**"*. Post-P1 the rule fires only in **≥2-actor-type** sessions, so both lines become over-broad/false. The FINAL plan's edit list is `CHANGELOG.md:24,45` + `decision_tools.py`/`session_tools.py` docstrings (P1) and ADR-index/CONTRIBUTING/spec-§4.4 (P6) — **neither names ADR 002 D1:32 / :100**. The ADR is the canonical rationale home (L11.6's entire purpose); a stale D1 re-creates the G5 doc/code-contradiction class the plan claims P1 closes. **This exact dilution was raised by Round-2 exhaustiveness (A-F3 / M1 / §4 / §5 table row "ADR 002 D1 line 32 reconcile … ❌ P1 says docstrings — does not name ADR 002 D1") and was NOT folded into the FINAL plan.** Fix: add `docs/adr/002-…:32` (+ `:100`) to P1's doc-truth-up list (sequenced WITH the P1 code), or to P6.

### 5.2 P9b locked-region citation under-specifies the lost-update window (low-severity precision)
FINAL P9b: *"minimal portable cross-process lockfile around shared `~/.trace/knowledge/` read-modify-write (`store.py:75-106` atomic-but-UNLOCKED)"*. `store.py:75-106` is `save_store` (write half) only. The lost-update window is the whole `load_store → mutate → save_store` span, which executes in `extensions/learn/__init__.py` (`_recall_hook` `:103-118`, `_extract_hook` `:121-130`, `trace_learn_add` `:204-232`, `trace_learn_forget` `:274-278` — each `store.load_store(...)` … `store.save_store(...)`). A lock confined to `save_store` serializes writers but a concurrent process can still `load_store` stale state before the holder's `save_store` completes → lost update persists. The plan's *intent* (mandate a minimal lockfile, last-writer-wins is unacceptable) is right and Round-2 confirmed it; only the file:line scope is too narrow. Fix wording: lock the load-modify-save critical section (acquire before `load_store`, release after `save_store`), not merely "around `store.py:75-106`". (Round-2 correctness §3/P9(b) + exhaustiveness M6 both noted the wider surface incl. the non-atomic `.npy` at `store.py:104`/`save_embeddings_cache`.)

### 5.3 P5 omits the new dev dependency (very low severity)
A real PROV-O parser round-trip test needs `rdflib` or `pyld`; `pyproject.toml:29-38` `dev` has neither. Zero runtime risk (test-only), but the plan bullet should state "add `rdflib`/`pyld` to `[project.optional-dependencies].dev`". Round-2 correctness §3/P5 raised this; still unstated.

### Not bugs (confirmed correctly folded in / safe)
- P1 ai→ai carve-out: **correctly folded** — the FINAL plan explicitly says guard ONLY the non-ai branch + keep ai→ai unconditional (A1 line 47); the code structure (`:86-91` vs `:92-99`; `:320-323` vs `:330-334`) genuinely supports this; the ai→ai regression-guard test passes and survives.
- M2 = exactly 2 tests: **correct** — `test_v041_attribution_audit.py`'s `_make_session` is 2-participant, so its detector/render-order tests survive; the bug-encoding surface is precisely the 2 named tests (verified by running them).
- P2 `:340`-already-guarded, P4 delete-the-duplicate, P9b mandated, spec §3.6 do-not-touch: **all correctly folded** per the Round-2 corrections.
- No version bump; sequencing P1→P2→P3 (with P3 internally after P1) → P4–P6 → P9 → P7-green-gated: sound.

---

## Bottom line

**GO-WITH-FIXES (~90%).** Every P-item is implementable against the real current code, the outcome-determining Round-2 corrections (P1 ai→ai carve-out, M3=actor-types, M2=2-tests, P2 `:340`, P4 delete-duplicate, P9b mandate) are correctly folded in, M3/`evt_016` is correctly resolved and properly escalated to the user, and the P3 counts are **exactly** 28 / 15 / 1 / 2 (independently JSON-verified, and the waggle session survives P1's type-guard so the L9.1 gate holds). The only remaining issues are three concrete unstated steps of the same doc/scope-precision class the 3-round process has been converging on — diminishing returns, none blocking the GO direction: (1) add `docs/adr/002-…:32`+`:100` to P1/P6's doc-truth-up list (a Round-2-flagged dilution not folded in), (2) widen P9b's locked region from "`store.py:75-106`" to the full load→mutate→save span, (3) note P5's new dev dependency. Fix those three in-plan and implement; the implementation's own failing tests will surface anything finer.
